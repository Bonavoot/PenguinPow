/**
 * Text animation timing for counter grab (LOCKED!), grab break (BREAK!), and perfect parry (PERFECT) hit effects.
 * All use these values so the centered text duration is consistent.
 */
export const HIT_EFFECT_TEXT_DURATION = 0.6; // seconds - total animation length
export const HIT_EFFECT_TEXT_DELAY = 0.05;   // seconds - delay before text pops in
