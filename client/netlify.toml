[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Use committed public/baked/ — regenerating hat WebPs exceeds build time limits.
  SKIP_BAKE = "1" 